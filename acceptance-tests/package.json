{
  "name": "acceptance-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Browser-based acceptance test suite for the application under test.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/node": "^22.7.0",
    "dotenv": "^16.4.5",
    "typescript": "^5.6.0"
  }
}
